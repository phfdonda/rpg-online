function mapSections(data, section, game){
  const {Characteristics, Attributes, Abilities } = data

  const titles = []

  const map = {
    labels: Object.keys(data[section]),
    sectionContent: data[section]
  }


}

export default mapSections